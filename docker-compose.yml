version: "3"
services:
  supman-docker:
    build:
      context: .
      dockerfile: Dockerfile
    image: supman-docker
    container_name: supman
    working_dir: /var/www
    restart: always
    ports:
      - "8282:80"
    networks:
      - supman

networks:
  supman:
    driver: bridge